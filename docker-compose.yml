services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - smailo-data:/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=/data/smailo.sqlite

volumes:
  smailo-data:
